; AI Summary: Provides tox configuration to build the svbmc wheel in an isolated
; environment and run the test‑suite across Python 3.10‑3.12, mirroring CI.
[tox]
envlist = py311, py312
isolated_build = true
skip_missing_interpreters = true

[testenv]
deps =
    pytest
commands = pytest -q
